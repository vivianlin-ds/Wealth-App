{
  "name": "wealth-allocation-tracker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "build:start": "npm run build && python3 backend/server.py",
    "dev": "python3 backend/server.py",
    "start": "python3 backend/server.py"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
